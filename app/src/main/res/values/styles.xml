<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/wikitude_orange_light</item>
        <item name="colorPrimaryDark">@color/wikitude_orange_dark</item>
        <item name="colorAccent">@color/wikitude_orange_light</item>
        <item name="colorControlNormal">@color/wikitude_orange_dark</item>
        <item name="colorControlActivated">@color/wikitude_orange_dark</item>
        <item name="colorControlHighlight">@color/wikitude_orange_dark</item>
        <item name="colorSwitchThumbNormal">@color/wikitude_orange_dark</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
